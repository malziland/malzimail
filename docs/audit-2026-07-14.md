# LANGAUDIT — Release-Gate malziMAIL, 2026-07-14

Read-only-Audit (Multi-Agent: Security · Privacy/State · Ops/Delivery · Docs),
konsolidiert und gegengeprüft. Kein Code wurde während des Audits verändert;
die anschließend umgesetzten Doku-/CI-Korrekturen sind unten je Finding vermerkt.

## 1. Provenienz

| Feld | Wert |
|---|---|
| Commit | `b6c99a1` (HEAD zum Audit-Start) |
| Branch | `main` |
| Letzter Tag | `v1.0.1` (⚠️ `v1.1.0` ist live, aber ungetaggt — siehe OPS-2) |
| Working Tree | sauber zum Audit-Start |
| Profil / Tiefe | Serverless/Edge · SERVICE_API + UI · `RELEASE_GATE` |
| Kritikalität / Daten | produktiv, öffentlich · PII (Mail-Inhalte) + Credentials (Wegwerf-Logins) |

## 2. Release-Entscheidung

| Gate | Ergebnis |
|---|---|
| REPO_READY | **CONDITIONAL** — 1× P1 (CI-Blocker) behoben; P2-Laufzeitbefunde dokumentiert, Fix ausstehend |
| PRODUCTION_READY | **CONDITIONAL** — offene P2-Security/Reliability-Punkte (dev-getesteter Fix nötig); kein P0 |

Kein P0. Der Doku-/CI-Patch (v1.1.1) ändert **kein** Laufzeitverhalten und kann
den Live-Dienst nicht verschlechtern. Die Laufzeitbefunde bestehen bereits im
Live-Stand v1.1.0 und werden über eine separate, dev-getestete Änderung behoben.

## 3. Findings (nach adversariellem Gegen-Check)

Legende Status: ✅ behoben (in diesem Durchgang, sicher) · 🔧 offen, dev-getesteter Fix geplant · 📄 Doku

### P1
| ID | Titel | Status |
|---|---|---|
| OPS-1 | Secret-Scan (gitleaks) schlägt auf **jedem PR** fehl (`GITHUB_TOKEN` fehlt seit gitleaks-action v3) → alle 6 Dependabot-PRs rot, Update-Pipeline blockiert. Push-Läufe auf main blieben grün, daher zunächst unbemerkt. | ✅ `GITHUB_TOKEN` + `permissions: contents:read` ergänzt, Actions SHA-gepinnt |

### P2
| ID | Titel | Status |
|---|---|---|
| SEC-1 | **Intra-Workshop-Einsicht:** Postfach-Lesen hängt am geteilten Workshop-Token; Google-Adressen sind kurze `Wort+4 Ziffern`-Logins, `/api/address/status` bestätigt Existenz tokenlos → Teilnehmer kann fremde Postfächer desselben Workshops erraten/aufzählen und mitlesen. Widerspricht der Zusage „nur eigene Postfächer“. | 🔧 dokumentiert; Fix braucht Design-Entscheidung (Cross-Device-QR) |
| OPS/STATE-1 | **„Stopp“ scheitert bei Google-Ausfall:** `wipeAllSessions` löscht Google **vor** lokal; Token-Abruf ohne try/catch → bei Google-/Netz-Ausfall wird lokal nichts gelöscht, Fehler wird verschluckt, Dashboard zeigt weiter „läuft“. | 🔧 offen |
| PRIV-1 | **Login-IPs dauerhaft:** `loginguard:<ip>` je Fehlversuch in `settings`, nie gelöscht → Widerspruch zur Datenschutzseite („keine IP dauerhaft“) + unbegrenztes Wachstum. | 🔧 offen |
| SEC-2 | **`ADMIN_KEY`-URL-Zugang:** `/admin?key=…` umgeht die Login-Drossel, Vergleich nicht konstante-Zeit; im Secret-Inventar bisher nicht geführt. Nur aktiv, wenn das Secret gesetzt ist. | 📄 dokumentiert (Inventar + Rest-Risiko); 🔧 Härtung/Entfernung offen |
| DOC-1 | SECURITY-MODEL nannte PBKDF2 **600k** (real 100k) und Cookie **Strict** (real Lax). | ✅ korrigiert |
| DOC-2 | `security-audit-2026-06-14.md` ohne Überholt-Hinweis (4 veraltete Aussagen). | ✅ Überholt-Banner ergänzt |
| OPS-2 | `v1.1.0` (live) ohne Git-Tag → Rollback-Weg 2 & Reproduzierbarkeit des Live-Stands lückenhaft. | 🔧 Tag im Release-Schritt nachziehen |
| OPS-3 | GitHub-Actions per beweglichem Major-Tag statt Commit-SHA gepinnt. | ✅ auf SHA gepinnt |

### P3
| ID | Titel | Status |
|---|---|---|
| PRIV-2 | Google-Konto-Leiche bei Doppelfehler (Kompensations-Delete prüft `false`-Rückgabe nicht). | 🔧 offen |
| STATE-2 | Fixer 48-h-Cap löscht Mails aus aktiven Postfächern, wenn `address_ttl_hours` > 48. | 🔧 offen (nur Nicht-Default) |
| STATE-3 | Doppelklick „Start“ ohne Transaktion → theoretisch zwei aktive Links (selbstheilend). | 🔧 offen |
| OPS-4 | Cron holt Google-Token je Konto (N+1) statt Batch → Verzug bei vielen Konten. | 🔧 offen |
| STATE-4 | „Stopp löscht sofort alles“ — abgelaufene Reste warten auf Cron (≤ 6 h). | 📄 Wortlaut präzisiert |
| OPS-5 | Kein `permissions:`-Block im Workflow (durch Repo-Default read gemildert). | ✅ ergänzt |
| DOC-3 | `ADMIN_TRAINER_TOKEN` (Legacy) nicht im FLAGS-Register. | ✅ ergänzt |
| DOC-4 | Coverage-Zahl uneinheitlich (AGENTS 80 % vs. real 90/90/90/80). | ✅ vereinheitlicht |

### Bewusste Trade-offs (kein Finding)
- Kein Rate-Limit auf `/api/address` (dokumentiert, Betreiber-Verantwortung).
- Betreiber kann entschlüsseln (systembedingt).
- Mail-iframe `allow-popups-to-escape-sandbox` (üblicher Mail-Client-Kompromiss; kein Skript-Exec, keine externen Ressourcen).
- CI `npm install` statt `npm ci` (ADR-0001, Wiedervorlage nächstes LANGAUDIT).

## 4. Extern verifiziert (read-only, 14.07.2026)
- Branch Protection auf `main`: **nicht gesetzt** (`gh api …/branches/main/protection` → 404 „Branch not protected“). Externe Kontrolle, vom Betreiber zu setzen.
- Repo-Default-Workflow-Rechte: `read`. Owner-Typ `User` → gitleaks braucht keinen Lizenzschlüssel.
- Keine Secrets/`.dev.vars`/`wrangler.jsonc`/`docs/links.md` im Repo oder in der (bereinigten) Historie.
- Lockfile: 0 Nicht-npmjs-Quellen; Produktions-Deps mit Integrity-Hashes.

## 5. Geprüft, unauffällig
SQL nur in `db/queries.js` (gebundene Parameter) · HTML-Escaping server- und clientseitig · CSP ohne `unsafe-inline` (Theme-Hash) · Mail-iframe-Sonder-CSP wie dokumentiert · SSRF: nur feste Google-Endpunkte · keine PII/Secrets in Logs (Adressen gehasht) · Adress-Erzeugung race-frei (`INSERT OR IGNORE`+Retry) · Cron-Google-Löschung idempotent · Zeit durchgängig ms, JWT in s · Migrationen 0001–0006 lückenlos · Dependabot deckt npm + actions.

## 6. Sanierungsfahrplan
- **Sofort (dieser Durchgang, sicher):** OPS-1/3/5, DOC-1..4, SEC-2-Doku, STATE-4-Wortlaut → im Patch v1.1.1.
- **Als Nächstes (eigene, dev-getestete Änderung, ein Deploy):** PRIV-1 (IP-Aufräumen), OPS/STATE-1 (Stopp lokal-zuerst), SEC-2-Härtung, PRIV-2. Vor Live: dev-Instanz + Verhaltens-Check.
- **Mit Design-Entscheidung (Christoph):** SEC-1 (Postfach-Bindung ohne Bruch des QR-Cross-Device-Logins).
- **Gelegenheit:** STATE-2/3, OPS-4.
- **Extern (Christoph):** Branch Protection + Required Checks auf `main`; 2FA.
